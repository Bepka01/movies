{
  "name": "movies",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "prettier --check ."
  },
  "devDependencies": {
    "eslint": "^10.0.3",
    "gh-pages": "^6.3.0",
    "prettier": "3.8.1",
    "vite": "^7.3.1"
  }
}
